import {
  FETCH_ALL_CARDS,
  FETCH_ALL_CARDS_SUCCESS,
  FETCH_ALL_CARDS_FAIL,
} from '../actions/types';

const INITIAL_STATE = {};

export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
